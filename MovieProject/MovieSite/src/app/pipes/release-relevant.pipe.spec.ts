import { ReleaseRelevantPipe } from './release-relevant.pipe';

describe('ReleaseRelevantPipe', () => {
  it('create an instance', () => {
    const pipe = new ReleaseRelevantPipe();
    expect(pipe).toBeTruthy();
  });
});
