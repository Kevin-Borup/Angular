import { IfModDirective } from './if-mod.directive';

describe('IfModDirective', () => {
  it('should create an instance', () => {
    const directive = new IfModDirective();
    expect(directive).toBeTruthy();
  });
});
